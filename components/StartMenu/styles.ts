import styled from '@emotion/styled';

export const Section = styled.section({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  margin: '2rem auto',
  textAlign: 'center'
});
